# Development Environment Configuration
NODE_ENV=development
PORT=3000
APP_NAME=CYNERZA Backend API (Development)
APP_VERSION=1.0.0

# Database - Local MongoDB
MONGODB_URI=mongodb+srv://lucifer_7:Adi9438@lucifer.exygh60.mongodb.net/

# Security - Development only (NOT for production)
# Generate secure secrets: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_ACCESS_SECRET=dev-access-secret-change-for-production-use-crypto-random-bytes-example
JWT_ACCESS_EXPIRATION=15m
JWT_REFRESH_SECRET=dev-refresh-secret-change-for-production-use-crypto-random-bytes-example
JWT_REFRESH_EXPIRATION=7d
BCRYPT_ROUNDS=10

# Rate Limiting - Login attempts
LOGIN_ATTEMPT_LIMIT=5
LOGIN_ATTEMPT_WINDOW=900

# OTP Configuration
OTP_LENGTH=6
OTP_EXPIRATION=300
OTP_MAX_ATTEMPTS=3


# CORS - Allow localhost origins
CORS_ORIGIN=http://localhost:3001,http://localhost:3002,http://localhost:5173
CORS_CREDENTIALS=true

# Rate Limiting - More permissive in development
THROTTLE_TTL=60
THROTTLE_LIMIT=1000

# Logging - Verbose for development
LOG_LEVEL=debug
LOG_FORMAT=pretty

# Swagger - Enabled in development
SWAGGER_ENABLED=true
SWAGGER_PATH=api/docs

# Health Check
HEALTH_CHECK_ENABLED=true
DISK_THRESHOLD_PERCENT=90
MEMORY_HEAP_THRESHOLD=300MB
